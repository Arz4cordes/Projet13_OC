version: '2.1'
orbs:
  win: circleci/windows@3.0
jobs:
  build:
    executor:
      imagename: windows-server-2022-gui
      name: win/windows
      size: medium
      version: edge
    steps:
      - checkout
      - run: 'Write-Host ''Hello, Windows'''
workflows:
  my-workflow:
    jobs:
      - build